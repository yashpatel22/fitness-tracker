// App version — bump this on EVERY release so you can confirm which build is
// running on a device (shown on the Profile screen). Keep code-app and web-app
// in sync when a release ships to both.
export const APP_VERSION = '1.5.0';
