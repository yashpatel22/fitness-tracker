// App version — bump this on EVERY release so you can confirm which build is
// running on a device (shown on the Profile screen). Each app (web-app / code-app)
// versions independently, since they ship separately and can differ in features.
export const APP_VERSION = '1.6.0';
